import React from 'react';
import { defineComponent, h } from 'vue';
import remoteReactButton from 'remote_react/button';

export default defineComponent(() => {
  return () => <remoteReactButton />;
});
